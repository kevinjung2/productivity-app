<%= form_for @item do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.label :description %>
  <%= f.text_area :description %>
  <%= f.label :category %>
  <%= f.collection_radio_buttons :category_id, @categories, :id, :name %>
  <%= f.submit %>
<% end %>
