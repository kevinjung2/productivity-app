<% @users.each do |user| %>
  <h3><%= link_to user.name, user_path(user) %></h3>
  <ul>
    <li>Hosting <%= user.hosted_events.count %> events</li>
    <li>Attending <%= user.events.count %> events</li>
  </ul>
<% end %>
